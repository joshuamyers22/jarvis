"""Job bodies.

Nothing in this package imports Airflow. Every module here is runnable from a
terminal, from a notebook, or inside a batch container. That is what makes a
failing task debuggable directly instead of through the scheduler.
"""
